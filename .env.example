# Copy to `.env.local` and fill in values. Do not commit `.env.local`.
# Registry-iQ (Supabase) — same project as TLC iQ Property Registry in the platform.

REGISTRY_IQ_SUPABASE_URL=
REGISTRY_IQ_SUPABASE_SERVICE_ROLE_KEY=

# Optional: scripts that hit Airtable or other services
# AIRTABLE_API_KEY=
# AIRTABLE_BASE_ID=
