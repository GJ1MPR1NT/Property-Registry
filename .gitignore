# Dependencies
node_modules/

# Secrets — never commit
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Logs and temp
*.log
npm-debug.log*

# Dropbox conflict copies
*conflicted copy*

# Local scratch
.cursor/
.cache/

# Optional local spreadsheets (not part of source)
*.xlsx
.firecrawl/

# Local DB / Access artifacts (data lives in Supabase, not git)
*.accdb
Access/*.db

# Local marketing image drops (canonical copies in Cloudinary / Box)
HUB Bloomington/
HUB Raleigh/
